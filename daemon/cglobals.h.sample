#ifndef _globals_h_
#define _globals_h_

double max_load = 10.00;

int daemon_interval_high_load = 30;

// Generic interval for use with errors or reconnections
int daemon_interval_generic = 20;
int daemon_interval_process = 20;
int daemon_interval_ssh_key = 5;

#endif
